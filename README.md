# Nao-Webots

## Requirements

The world file was developed in Webots version R2023b.
**Note**: These scripts are developed in **Python version 3.9.12**. Ensure you're using this or a compatible version to avoid compatibility issues.

Ensure you have the following libraries installed before running the scripts:

```bash
pip install pandas numpy minisom
```
